פרויקט אדריכלית
טכנולוגיות: React, Node.js, postgreSQL, Prisma
למה ריאקט? 
שפה קלה לעיצוב ולשימוש חוזר
למה נוד? 
שפה פשוטה וקלה להתממשקות עם כלים שונים
למה PostgreSQL? 
הוא DB רלציוני ומתאים במיוחד להתממשקות עם נוד
למה ORM הוא  Prisma? 
יש הרבה קשרים וטבלאות ולוגיקה מורכבת 
בprisma כל שינוי עובר דרך Migration מתועד וניתן לשחזור

חלק 1 – הסבר כללי על הפרויקט
שם הפרויקט: אתר תדמית + אזור אישי ללקוחות עבור אדריכלית ומעצבת פנים
מטרות הפרויקט:
אתר תדמית ציבורי להצגת עבודות האדריכלית והפרויקטים שלה.

אזור אישי לכל לקוח, עם פרויקטים, צ’אט, העלאות והסברים מהאדריכלית.

לאפשר לאדריכלית לנהל את האתר והלקוחות באופן עצמאי ופשוט.

סוגי משתמשים:

מבנה האתר ואזורי ניהול
1. אתר תדמית (Public)
גלוי לכל המבקרים, לא דורש התחברות

כולל:

אודות

שירותים

צור קשר

פרויקטים ציבוריים

האדריכלית יכולה להוסיף/לערוך/למחוק פרויקטים ציבוריים, לשנות טקסטים ותמונות, ולסדר סדר הצגה.

2. אזור אישי ללקוחות (Private)
חובה התחברות

לכל לקוח:

כניסה לפרויקט אחד אוטומטית או רשימת פרויקטים לבחירה

ציר זמן עם תוכניות והיסטוריה של מה שבוצע

קבצים שהאדריכלית העלתה (PDF, תמונות)

צ’אט צף עם האדריכלית

לקוח יכול לשלוח קבצים רק בצ’אט

3. דשבורד האדריכלית
בחירה ראשונית: ניהול אתר תדמית / ניהול אזור אישי

דשבורד אתר תדמית:

ניהול פרויקטים ציבוריים

ניהול טקסטים ותמונות

סדר הצגה

דשבורד אזור אישי:

ניהול לקוחות

ניהול פרויקטים אישיים

הצ’אט (צף)

העלאות / הורדות קבצים

ציר זמן / שלבים / דדליין

התראות / סטטוסים


צ’אט
צ’אט אחד לכל לקוח

צף ופתוח לאורך כל הניווט בין מסכים

קבצים נשמרים בהיסטוריית הצ’אט בלבד

אפשר להוסיף כותרת בצ’אט לכל שינוי בציר הזמן


ציר זמן
ציר זמן מתוכנן: מה האדריכלית מתכננת

ציר זמן היסטורי: מה שבוצע בפועל

כל העלאה / שינוי מקבל כותרת בציר הזמן



תיאור מערכת – גרסת מנטור וגרסת מעצבת
גרסת מעצבת
מבנה האתר ותצוגה
אתר תדמית (Public)
נגיש לכל משתמש ללא התחברות.
מציג רשימת פרויקטים ציבוריים.
כל פרויקט כולל תמונות, תיאור, סטטוס פרסום.
האדריכלית יכולה לנהל את הפרויקטים: להוסיף, להסיר, לשנות.
אזור אישי של לקוחות (Client Area)
נגיש רק למשתמשים עם הרשאות (login required).
לכל לקוח יש פרויקט אחד או יותר; אם רק אחד – נפתח מיד, אם יותר – יש אפשרות לבחור.
התצוגה כוללת:
צ'אט עם האדריכלית – היסטוריית שיחות אחת לכל לקוח, כולל קבצים (PDF, תמונות וכו'), שמורים בהיסטוריה בלבד.
תוכן מונגש ללקוח – תיקיות תוכן נפרדות לכל פרויקט, שבתוכן האדריכלית מעלה קבצים, מדיה והסברים.
ציר זמן משוער – מה צפוי להתרחש ומה כבר בוצע.
האדריכלית יכולה להוסיף תוכן חדש, להגדיר סדר, לערוך ולהסיר.
דשבורדים
דשבורד כללי – ניהול האתר כולו.
דשבורד אזור אישי – ניהול לקוחות, פרויקטים פרטיים, צ'אט ותוכן.
גרסת מנטור
ארכיטקטורה והנדסת נתונים
טבלת Users
שדות: id, email, password, role (Enum: 'admin', 'client', 'viewer')
מטרתה לנהל התחברות וגישה.
טבלת Clients
שדות: id, userId (FK), name, contactInfo
מגדירה לקוח ושייכות ל-User.
מאפשרת למשתמש אחד (User) לייצג מספר לקוחות בעתיד.
טבלת PublicProjects
שדות: id, title, description, isPublished, createdAt, updatedAt
נפרדת לחלוטין מהפרויקטים האישיים, למניעת חפיפה ואבטחת מידע.
טבלת ClientProjects
שדות: id, clientId (FK), title, status, createdAt, updatedAt
קשר 1:N עם Client.
תוכן ותקיות (ContentGroup & ContentItem)
ContentGroup: id, clientProjectId (FK), title, order
ContentItem: id, contentGroupId (FK), type, title, description, fileUrl, order
`ContentGroup` מייצג תיקיה/קטגוריה של תוכן בפרויקט, ואינו חלק מציר הזמן.
`ContentItem` מייצג פריט תוכן בתוך תיקיה, ויכול להיות קובץ, תמונה, וידאו, PDF או טקסט.
מומלץ לשמור גם שדות metadata בהמשך כגון `mimeType`, `originalFileName`, `size`, `thumbnailUrl` לפי צורך.
ציר זמן (TimelineItems)
PlannedTimelineItem: id, clientProjectId, title, expectedDate, order
ActualTimelineItem: id, clientProjectId, plannedTimelineItemId (optional), title, actualDate
ציר הזמן הוא ישות נפרדת מהתוכן. הוא מתאר שלבים, סטטוסים והתקדמות, ולא מחליף את תיקיות התוכן.
צ'אט (Chat & ChatFiles)
Chat: id, clientId (FK), messages (linked to ChatMessage)
ChatMessage: id, chatId (FK), senderRole, text, createdAt
ChatFile: id, chatMessageId (FK), fileType (Enum), filePath
שמירת היסטוריית שיחות אחת לכל לקוח, לא משתנה בין פרויקטים.
אם בעתיד יהיה צורך לקשר הודעה לפרויקט מסוים, עדיף להוסיף שיוך אופציונלי של הודעה לפרויקט במקום לפצל כבר עכשיו לצ'אט נפרד לכל פרויקט.
שיפורים שהומלצו
להפריד את התמונות בפרויקטים לטבלת ProjectImages כדי לכלול Alt text וסדר הצגה.
Enums בשדות FileType וב-Role לניהול קל וברור ב-Prisma.
סדר (order) בשדות ContentGroup ו-PlannedTimelineItem חשוב להצגה נוחה ואחידה.




 
מבנה הפרויקט
server/ בשביל שיוכלו להריץ שרת בנפרד.                        
│─ src/ פה נמצא כל הקוד האמיתי שירוץ. בלי קונפיגורציה ומסד נתונים, רק לוגיקה.                        
│   ├─ app.ts מחבר את כל החלקים.            Express, middlewares, routes  
│   ├─ server.ts מפעיל את השרת, מאזין לפורט. מופרד ממה שמעליו בשביל ליצור אפשרות לבדיקות.        
│   ├─ routes/כמו מפת דרכים של ה             API   
│   ├─ controllers/מתרגם בין העולם החיצוני למערכת     
│   ├─ services/כאן מבצעים את הלוגיקה הרצינית במערכת        
│   ├─ middlewares/שומרים שרצים לפני שהבקשה מגיעה לקונטרולר   
│   └─ utils/עוזרים קטנים- פונקציות כלליות. לשים לב! לא כל דבר שלא יודעים איפה לשים זה המקום           
│
│─ prisma/שכבת הנתונים.                        
│   └─ schema.prisma
│
│─ .env                       (לא נכנס לגיט)   סודות – סיסמאות, חיבור לDB  
│─ package.json
│─ tsconfig.json 
ציר זמן נכון לפרויקט שלך (מא’ עד ת’)
שלב 0 – אפיון וארכיטקטורה (עכשיו)
📌 אין קוד, אין דומיין
מה עושים:
הגדרת פיצ’רים

החלטה על טכנולוגיות 

תכנון DB

תכנון הרשאות

פירוק למשימות

למה:
שינוי פה עולה אפס

שינוי אחרי דומיין/פרודקשן = כאב ראש

⏱️ זמן: קצר אבל קריטי

שלב 1 – פיתוח מקומי (Localhost)
📌 עדיין בלי דומיין
מה עושים:
Backend (API, DB, Auth)

Frontend (מסכים עיקריים)

חיבור ביניהם

עבודה עם .env

העלאת קבצים ל־local / mock

למה:
80% מהבאגים קורים כאן

לקוחה לא צריכה לראות כלום עדיין

⏱️ זה השלב הארוך

שלב 2 – MVP עובד (לפני לקוחה)
📌 עדיין בלי דומיין
מה חייב לעבוד:
אתר תדמית בסיסי

התחברות

Admin בסיסי

אזור אישי ראשוני

מה כן עושים פה:
דיפלוימנט זמני

כתובת זמנית (לדוגמה: משהו של Vercel / Render)

למה:
לראות שזה עובד מחוץ למחשב שלך

בלי להתחייב לדומיין


🚨 רק כאן קונים דומיין
שלב 3 – לפני מסירה ללקוחה
📌 זה הזמן הנכון לקנות דומיין
למה דווקא כאן:
שם האתר כבר סגור

המבנה כבר קיים

אין סיכון לזרוק דומיין לפח

אפשר לחבר מיילים, SEO, וכו’

מי קונה:
הלקוחה (חשוב!)

או את – אבל על שמה ובאישורה

מה עושים עם הדומיין:
מחברים ל־Frontend

מגדירים DNS

HTTPS

מיילים אוטומטיים (notifications)


שלב 4 – פרודקשן אמיתי
📌 עם דומיין
מה עושים:
העלאת גרסה יציבה

בדיקות

הרשאות

הרשאות קבצים

גיבויים

דוקומנטציה


סוג משתמש	מה רואה / יכול לעשות
	
מבקר	רואה אתר תדמית בלבד (אודות, שירותים, פרויקטים ציבוריים). אין התחברות.
לקוח	רואה אזור אישי עם: פרויקט אחד או רשימת פרויקטים, ציר זמן, קבצים שהאדריכלית העלתה, צ’אט פתוח, יכול לשלוח קבצים בצ’אט בלבד.
אדריכלית	משתמשת יחידה עם כל ההרשאות: ניהול אתר תדמית (פרויקטים ציבוריים, טקסטים, תמונות, סדר הצגה), ניהול אזור אישי (לקוחות, פרויקטים אישיים, צ’אט צף, העלאות קבצים, ציר זמן, סטטוסים ודדליין).

